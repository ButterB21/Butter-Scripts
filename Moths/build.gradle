plugins {
    id 'java'
}

version = '2.1'

tasks.named('jar') {
    manifest {
        attributes(
                'Implementation-Title': 'Moths',
                'Implementation-Version': project.version
        )
    }
}